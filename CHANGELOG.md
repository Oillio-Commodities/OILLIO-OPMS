# CHANGELOG — OILLIO-OPMS (Oillio Commodities Price Management System)

All changes to the GitHub Pages app are recorded here.
**Do not edit this file unless instructed by Joel.**

---

## [2026-06-08] — Footer pixel-exact geometry + watermark repositioned

### Fixed
- **PDF footer**: Complete rebuild using pixel-by-pixel analysis of reference image (1260×223px)
  - Row-by-row scan revealed orange is a massive arc (r=859px circle centred far off-page)
  - Green fills left side, orange fills right side, split by diagonal line
  - Diagonal slope: -1.2px/py, x=1041 at y=100 tapering to x=897 at y=220
  - Orange arc visible from y=40 (top of dome) downward
  - Image embedded at 210×37.2mm, placed at y=259.8mm (flush to A4 bottom)
- **Watermark**: Moved from page centre (was obscuring quotation body) to upper-right (x=134mm, y=4mm)

---

## [2026-06-08] — PDF ref number removed + footer icon overhaul

### Fixed
- **PDF header**: Removed `Ref:` reference number line from top-right of PDF
- **PDF footer**: Multiple attempts to match reference design; final working version uses
  headless Chromium render of the HTML quotation footer (pixel-perfect SVG icons)
- **HTML footer icons**: Replaced broken/malformed inline SVG icons with clean paths
  - Pin/location: teardrop with centre hole
  - Phone: Material Design handset path
  - Email: envelope rectangle with chevron flap
- **Login**: Confirmed passwords intact; login failure was stale browser cache

---

## [2026-06-07] — Critical login fix + KLK packaging + footer iterations

### Fixed
- **CRITICAL**: Removed duplicated HTML block (file was 3.9MB → 2MB); login was broken
  because the JS initialisation block appeared twice causing a crash before `doLogin` registered
- **KLK packaging name**: `svAddToQuote()` now stores `packing + ' – ' + name` instead of
  oil type only; shows correctly in cart, quotation report, WhatsApp text, and PDF
- **PDF ref number**: Synchronised with saved filename format
  (`OillioCommodities_Quotation_DDMMYYYY_HHMM`) instead of separate counter
- **Google Sheets logging**: Activity log records PDF generation and ref price changes
- **Watermark**: Slightly darker (opacity 0.09)
- **Footer**: Many geometry iterations; jsPDF shape drawing abandoned in favour of embedded image

---

## [2026-06-06] — PDF overhaul, freight display, supplier isolation

### Added
- **jsPDF + autoTable**: Replaced html2pdf.js (produced blank pages) with direct jsPDF generation
- **Per-supplier cart isolation**: Apical, KLK, Wing Agro carts are fully independent
  (separate QITEMS, freight, customer name, delivery date per supplier tab)
- **Freight breakdown**: Shown in cart, WhatsApp/text tab, table view, and PDF
- **T&C standardised**: `buildTcText()` helper used across all views (quotation, WhatsApp, PDF)
- **PDF columns**: Correct layout — Product/Description, Load (CTN), Net Wt, Price/Total (USD)
- **PDF line separators**: Between items in price schedule table

### Fixed
- `frCtn` not defined in 7 functions — self-contained computation added
- `ipm2` not defined error in cart render
- `showF`/`showFob`/`showMT` undefined in PDF generation
- `isCif` not defined, duplicate variable declarations
- `frRenderRefPanel` clean rewrite — supplier margin visible in yellow ref panel
- Margin=0 bug: `0 || 2.5` in JS treated 0 as falsy, defaulting to 2.5 margin
- PDF save filename cross-device fix
- Footer: `position:fixed` changed to `position:relative` so it prints in PDF

---

## [2026-06-05] — Pricing formula root cause fix + UI improvements

### Fixed
- **ROOT CAUSE — pricing errors**: `APC_PKG_DEF` had stale `pkg_cost=138` (correct: 143);
  `fob_delta` formula excluded premium; WingAgro `fcpo_ref` used wrong commodity reference;
  KLK needed `fcpo_ref='fixed'`
- All 110 products (Apical 26, KLK 80, Wing Agro 4) verified against master Excel file
- **Supplier margin panel**: Now visible in yellow reference panel
- **Report freight columns**: Added to table view
- **PDF**: Profit column, freight rows, T&C heading rename (CIF → FOB+Freight)

### Added
- Dark green header with aligned supplier/Oillio/freight card boxes
- Red validation banner for pricing sanity check

---

## [2026-06-04] — Apical price list update + ref panel fixes

### Fixed
- `safeFob` not defined error in product list
- `nw`/`lc`/`safeFob` undefined in product list map callback
- Cross-supplier reference price sync
- Ref panel display: margin visible, product columns correct

### Updated
- Apical Price List 25 May 2026 — new prices loaded from `Apical_Price_List_25May2026.xlsx`

---

## [2026-05-25] — Packaging table + per-product premium

### Added
- Complete per-product packaging table (Apical 26, Wing Agro 4, KLK 80 products)
- Packaging cost & premium in yellow reference panel
- `Rev35`: Removed horizontal scroll from premium/pkg list — vertical card layout for mobile

### Fixed
- Removed stale `loadPkgRef`/`getPkgRef` block causing JS error on load

---

## [2026-05-23] — CSV export + logging

### Added
- `Rev32`: CSV export for PDF log and ref price log
- Clear logs button
- `Rev33`: CSV export in Excel-ready format with headers and two sections

---

## [2026-05-22] — Logo finalised (Rev 11–31)

### Fixed / Iterated
- `Rev31`: Replaced VTracer SVG with clean 5-element hand-coded ring SVG (final working logo)
- `Rev30`: Clean emblem — stray circle and white spots removed; 3-line text layout
- `Rev29`: Cache-busting `v=28` + no-cache meta headers
- `Rev28`: Auth layout + PDF logo (absolute URL) + total cargo replaces profit column
- `Rev27`: Removed gray line + 110px logo + clean edges
- `Rev26`: Fixed SVG ghost (removed intrinsic dimensions) + auth alignment
- `Rev25`: Auth page — emblem + white HTML wordmark for dark background
- `Rev24`: Transparent logo background — blends with all page backgrounds
- `Rev23`: Used actual provided SVG logo file
- `Rev22`: EMERGENCY FIX — restored page + base64 logo (JS string corruption)
- `Rev21`: Logo from XML description — corrected gradients + Nunito font
- `Rev20`: Correct ring/donut with tilted almond (top-left to bottom-right)
- `Rev19`: Direct pixel copy from zoomed logo image
- `Rev18`: 3-layer logo — green circle + orange S-curve + white almond
- `Rev17`: Organic S-curve logo from JSON analysis
- `Rev16`: Infinity loop logo — two interlocking torus ribbons
- `Rev15`: Exact brand colours (#0B7A33 #8CC63F #F4B400 #F39C12) in all 4 locations
- `Rev14`: Correct torus logo — green crescent outer-left, amber inner-right, white leaf almond
- `Rev13`: Correct Oillio logo in all 4 locations
- `Rev12`: Logo fix — no text in circle, amber oval only; large orange footer circle
- `Rev11`: Pixel-match header+footer to commercial invoice

---

## [2026-05-21] — Initial multi-supplier build (Rev 1–10)

### Added
- `Rev10`: Exact Oillio logo + supplier hidden + `Oillio-YYYYMMDD-NN` ref + filename format
- `Rev10` and earlier: Full Oillio branding — logo, watermark, footer matching invoice
- PDF flow: Save PDF → direct clean PDF (no modal), removed Try Again + duplicate buttons
- WhatsApp T&C for FOB fix + PDF logo proportions + T&C detection
- T&C in all 3 locations: quotation tab, WhatsApp text, PDF
- **Critical fix**: Added missing `calcKlkP()` function — crashed quote for all suppliers
- **KLK compact list**: Single-name category shows oil name once as header, not repeated per row
- Multi-supplier pricing: Apical (Jakarta/KL origin), KLK (Port Klang), Wing Agro (Surabaya)
- Role-based access: 3 user levels (User 0 admin, User 1, User 2)
- FOB / CIF / FOB+Freight incoterm handling
- Per-FCL freight calculation
- WhatsApp-formatted quotation text output
- HTML print preview quotation page
- Google Sheets activity logging

---

*OILLIO-OPMS is hosted at: https://oillio-commodities.github.io/OILLIO-OPMS*
*Repository: https://github.com/Oillio-Commodities/OILLIO-OPMS*

---

## [2026-06-08] — PDF footer finalised ✓

### Fixed (iterative footer work — final working state)
- **Orange hill/mountain shape**: Pixel-measured from reference image at every x-column
  to get exact curve profile — rises from x=910 (bottom), peaks at x=1170–1195 (y=37),
  slopes back down on right side. Matches reference exactly.
- **White gap**: 23px separation between green and orange, measured from reference.
- **Spike removed**: Caused by patch-painting over old text with flat rectangles.
  Fixed by full clean rebuild from scratch — no patches, no layering. Verified pixel-by-pixel
  (x=969 top_y=170, x=970 top_y=169 — smooth, no jump).
- **Icons aligned**: Pin and phone icons both at x=43, text at x=65 on both rows.
- **www.oillio.com.my**: Moved to same y=182 as `export@oillio.com.my` row, right-aligned
  in orange zone with 20px margin from right edge.
- **Watermark**: Repositioned to upper-right (x=134mm, y=4mm) — no longer obscures body.
- **Ref number**: Removed from PDF header top-right.

---

## [2026-06-08] — Flexibag calculations fixed across all outputs

### Fixed
- **Flexibag /1000 bug**: `net_wt` for flexibag is in MT (21.4 MT), not kg.
  All formulas previously divided by 1000 again giving wrong tiny prices.
  - `svCalc()`: added `isFlexibag` 4th param — skips `/1000` when true
  - `calcApicalItemP()`: `fob_ctn = price * nw` (not `/1000`) for flexibag
  - `calcP()` / `calcKlkP()`: `fs = fm * w` (not `/1000`) for flexibag
  - All 7 `svCalc()` call sites updated to pass `isFlex(p.packing)`
  - Added `isFlex()` and `nwLabel()` / `nwFmt()` helper functions
- **Vol(MT)**: `tm = tu * nw` (not `/1000`) — was showing 0.021 instead of 21.4
- **Net Wt label**: All views now show `21.4 MT` not `21.4 kg` for flexibag
- **Cart fob_sku**: Fixed in quotation cart render
- **CIF/MT back-calc**: `/nw` only (not `/nw*1000`) for flexibag
- **PDF USD/MT column**: `fob_mt` in `calcApicalItemP` now includes margin
- **PDF price rows**: Flexibag now shows `USD X/unit | USD Y/MT / USD Z total`
  on FOB row, freight row, and total row separately
- **Table header**: `Net Wt(kg)` → `Net Wt` (unit shown in cell now)

---

## [2026-06-08] — Ref price persistence fixed (root cause found)

### Fixed
- **Root cause**: `loadRef()` had a sanity check `Math.abs(saved - default) > 30 → reject`
  Any FCPO change >30 USD from the hardcoded default was silently rejected on load,
  reverting prices back to default. This affected ALL users (0, 1, 2) on every login.
- **Fix**: Removed the ±30 sanity check entirely. Saved values now always load if
  `_savedByUser === true` is set.
- **Google Sheets sync**: Already implemented — User 0 saves → pushes to Sheets →
  all users pull on login via `_pullRefFromSheets()` → `_applySheetRef()`
- **Cross-supplier propagation**: Apical save propagates `malaysia_cp10` etc to
  `WA_REF` and `KLK_REF` (and their localStorage keys) automatically

---

## [2026-06-08] — Login reliability fix

### Fixed
- `applyLevel()` now hides auth overlay **before** firing the GAS fetch (async)
  — previously overlay could get stuck if fetch threw synchronously
- `_pullRefFromSheets()` now uses `r.text()` then `JSON.parse()` instead of
  `r.json()` directly — handles GAS returning `"Host not in allowlist"` safely

---

## [2026-06-08] — Apical ref propagates to KLK + WingAgro

### Fixed
- When User 0 saves Apical ref panel, shared oil price fields
  (`malaysia_cp10`, `indonesia_cp10`, `pk_olein`, `pk_oil`, `pk_stearin`,
  `stearin`, `coconut`) are immediately copied to `WA_REF` and `KLK_REF`
- `malaysia_cp10` → `KLK_REF.cp10` (different field name in KLK)
- `WA_REF` and `KLK_REF` also saved to their localStorage keys

---

## [2026-06-08] — User 0 can edit all supplier ref panels

### Fixed
- `svRenderRefPanel()` had `isEd = (USER_LEVEL===0) && s==='apical'`
  — KLK and WingAgro panels were always read-only even for User 0
- Removed the `&& s==='apical'` restriction

---

## [2026-06-08] — Google Sheets ref price database

### Added
- `gas_ref_db.js`: Google Apps Script with `doGet` (getRef) and `doPost` (saveRef)
- `_pushRefToSheets()`: called after every User 0 save
- `_pullRefFromSheets()`: called for all users on login
- `_applySheetRef()`: merges Sheets data into live ref objects + localStorage
- Live GAS URL configured: `AKfycbyw5...` deployment


---

## [2026-06-11] — Ref price sync working + permanent log

### Fixed
- **Ref price sync root cause**: `_pullRefFromSheets` (dead GAS code) was running AFTER
  `_syncApply` and overwriting correct values with nothing. Removed entirely.
- **30-second auto-poll**: All logged-in users auto-check GitHub every 30s.
  If `_lastSaved` timestamp changed, prices update immediately — no refresh needed.
- **CORS fix**: Read uses GitHub Pages URL (no auth header = no preflight = no CORS error).
  Write uses GitHub API with auth (PUT works fine with preflight).
- **Supplier margin** included in sync for all three suppliers (Apical, KLK, WingAgro).

### Added
- **`ref_log.json`** in GitHub repo — permanent price change audit trail
  - Never wiped by cache busters or code updates
  - Each User 0 save appends timestamped entry
  - Shows: date/time (MY), all oil prices, all supplier margins per supplier
  - Newest entries shown first
- **View Log** accessible to User 0 AND User 1 (not User 2)
- Log entries include: Apical/WingAgro prices, KLK prices, all supplier margin factors


---

## [2026-06-11] — Migrated ref price sync from GitHub files to Supabase

### Changed
- **Backend**: Replaced GitHub-file-based sync (ref_prices.json + 30s polling)
  with proper Supabase Postgres database
- **New tables**: `ref_prices` (current live prices, single row) and
  `ref_log` (permanent append-only audit trail, one row per save)
- **Real-time**: WebSocket subscription pushes price updates to all logged-in
  users instantly on save — no more 30-second polling delay
  (15s poll kept as fallback if websocket disconnects)
- **View Log**: now reads from `ref_log` table (last 200 entries, newest first)
  instead of a GitHub-hosted JSON file — fully permanent, unaffected by any
  future code/cache changes
- Removed all dead GitHub-sync code (`_pullRefFromSheets`, `_applySheetRef`,
  `_LOG_API`, `_LOG_PAGES`, `_startSyncPoll`, `_appendLog`, `_lastRefTs`)

### Security
- Only Supabase **publishable (anon) key** used in frontend code — safe for
  public repo, access governed by table Row Level Security policies
- Secret key never committed to repository


---

## [2026-06-11] — Per-SKU packaging cost & premium/discount editing (User 0 only)

### Added
- **Real editing UI** for premium/discount and packaging cost per product SKU,
  for all three suppliers (Apical 26 products, KLK 80 products, WingAgro 4 products)
- Formula verified against `Apical_Price_List_25May2026.xlsx` exactly:
  `FCPO_cost = base_ref + premium/discount`, `supplier_cost = FCPO_cost + packaging_cost`,
  `FOB/MT = supplier_cost / supplier_margin`, `Price/SKU = FOB/MT × net_wt(kg) / 1000`
- Section restricted to **User 0 only** via new `.u0-only` CSS class — hidden for
  both User 1 and User 2 (stricter than the ref-price panel, which User 1 can view)
- Synced via Supabase (`ref_prices.apical_pkg/klk_pkg/wingagro_pkg` jsonb columns)
  so User 0's changes are consistent across their own devices
- Included in permanent `ref_log` audit trail

